{
    // disables automatic formatting
    // if the .rettierrc configuration file is missing
    "prettier.requireConfig": true,
    "prettier.singleQuote": true,
    // when formatting, replace tab with spaces
    "prettier.useTabs": false
}
